# P1 验收报告（WZ/XCX 能力升级，2026-09-07）

> **验收性质**：独立验收会话，fail-closed，只判不修。一切以盘上事实为准，不采信施工会话口头声称。
> **规格源**：`docs/operations/WZ_XCX_能力升级方案_2026-09-07.md`（重点 §3 W-3/W-4、§4 全部、§5.3、附录 A）
> **前置复核**：`docs/operations/P0_验收报告_2026-09.md` 判定为 PASS；本会话抽查其关键结论未被 P1 改动破坏（见 §6）。
> **验收范围**：P1 三项 = ⑤ XCX 三项接线（X-1 `component_nday_screening` / X-3 备用解包器入 workflow 表 / X-4 `passive_leak_dork` 分支）⑥ input_testing 预算制（W-4，唯一动 Python 的点）⑦ JSFinder 接入 application_mapping（P1-6）+ §5.3 五个新工具登记（JSFinder / arjun / jwt_tool / wxapkg / FingerprintHub）
> **护栏遵守**：全程只读；`init_miniapp_engagement.py` 种子写入 `%TEMP%\xcx_p1_accept`；双 audit 后 `git status engagements/` 零改动；除本报告外未修改任何项目文件；零联网。

---

## 最终判定：**PASS（首轮 FAIL 仅 A3 一项；修复后复验通过，见 §0.1）**

- 首轮验收（2026-09-07 18:47 版 manifest）：A3 实测 `False`——`AGENT_MANIFEST.md` 缺 `JSFinder` / `jwt_tool` / `FingerprintHub`（`wxapkg` 的命中是既有项目脚本名的巧合，非新工具条目）。根因：manifest 由 `gen_agent_manifest.py` 从 `tool_strategy.json` 等 source 生成，**不转储 tool_registry.json**，而 `tool_strategy.json` 只新增了 arjun 引用（详见 §1.3，保留作为根因档案）。
- **修复后复验（19:21 再生 manifest）**：A3 五名齐备且均为 tool_strategy.json 的真实语义引用；登记四步回归全绿；全量回归绿；无 P2 范围污染；修复 git 变更恰好只有 `tool_strategy.json` + `AGENT_MANIFEST.md` 两个申报文件。
- 首轮已通过项（XCX 三项接线、input_testing 预算制、契约与引擎一致、双 audit 零违例、P0 结论完好）本轮复验均未回退。
- 修复交接提示词原文见 §8（已执行完毕，留档）。

---

## 0. 运行环境说明（验收事实的一部分）

| 项 | 值 |
|---|---|
| 仓库 | `D:\PythonSource\PythonProjects\PythonProject4`，分支 `chore/personal-offline-integrity-controls`（工作树含 P1 施工未提交变更） |
| PATH `python` | 3.14.4（`C:\Users\ASUS\AppData\Local\Python\pythoncore-3.14-64`），**无 pytest 模块** |
| 项目运行时 `.venv` | 3.14.4 + pytest 9.1.1（AGENTS.md 指定的新工具默认运行时） |
| 说明 | 与 P0 验收时环境一致：涉及 pytest 的命令（C2、E 组全量）用 `.venv` 运行；A1-A3、B、C1、D 与 skill-drift、双 audit 用 PATH `python` 即通过。环境事实，非 P1 缺陷。 |

### 0.1 修复后复验记录（2026-09-07 19:21-19:40，同验收会话）

| 复验项 | 实测 | 结论 |
|---|---|---|
| A3 验收命令重跑 | `all(...)` → **True**（JSFinder=T, arjun=T, jwt_tool=T, wxapkg=T, FingerprintHub=T）；manifest 生成时间 **2026-09-07 19:21** | 通过 |
| 引用真实性（非巧合命中） | `tool_strategy.json` 五名全 True，且均为真实语义引用：fingerprint notes += FingerprintHub（runtime=data 备库，本地比对）；application_mapping notes += JSFinder（"instead of JS-chunk sampling, run JSFinder … over collected JS chunks"，P1-6 fix 2026-09-07）；authenticated_testing notes += jwt_tool（离线解码/审计）；包处理 phase notes += wxapkg（备用解包器，与 xcx workflow §2 表 2b 同源语义）——首轮"wxapkg 巧合命中"假阳性已消除 | 通过 |
| 登记四步回归 | `rebuild_tool_inventory.py --check --json` → `violations: [], ok: true` EXITCODE=0；`check_skill_drift.py` → status ok 31 文件零漂移；`.venv pytest tests/test_tool_registry.py -q` → **43 passed**；manifest 再生（19:21） | 通过 |
| 全量回归 | `.venv pytest -q` → **1591 passed, 1 warning**（64.78s，与首轮同数无回退）；`verify_offline.py --json` → top status=ok；WZ audit jlc.com → `state=REVIEW_PENDING, issues=[]`（基线）；XCX audit zjlyxy → `state=CLOSED, issues=[]`；`git status engagements/` 零改动 | 通过 |
| P2 污染复查 | `signature_replay_l0` / `poc_intake` / `VP-20260907` 全库 grep 仍 0/0/0 | 通过 |
| 修复范围自检 | mtime 扫描（18:55 后）：git 跟踪文件中仅 `tool_strategy.json`（19:21）与 `AGENT_MANIFEST.md`（19:21）变更 = 申报范围；另两个晚于报告的文件均为 gitignore 本地数据（根 `oob_hits.jsonl` 为本地 OOB 监听日志，新记录 127.0.0.1 回环；`tools/miniapp_extract/zjlyxy_l4_extract_probe.py` 在 `/tools/*` 忽略区），不属 git 管理面 | 通过 |

**复验结论：A3 升格 PASS，整体判定由 FAIL 改判 PASS。**

---

## 1. A 组工具登记（A1/A2 PASS；A3 首轮 FAIL，复验后 PASS）

| 项 | 期望 | 实测 | 判定 |
|---|---|---|---|
| A1 注册表校验 | 退出码 0 | `--check --json` → `violations: []`, `ok: true`，EXITCODE=0 | **PASS** |
| A2 五工具状态 | 5 个全部 active；FingerprintHub runtime=data | 见下明细，全部一致 | **PASS** |
| A3 manifest 再生 | all(...) → True | 首轮 **False**（JSFinder/jwt_tool/FingerprintHub 缺失）；修复复验 **True**（§0.1，19:21 再生，引用真实） | **PASS（复验后）** |

**A2 实测明细**（`tools/tool_registry.json`，registry 现共 39 条目）：

| tool_id | status | runtime | version | path |
|---|---|---|---|---|
| JSFinder | active | python | master@2026-09 | tools/managed/jsfinder/JSFinder.py |
| arjun | active | python | 2.2.7 | tools/managed/arjun/python |
| jwt_tool | active | python | master@2026-09 | tools/managed/jwt_tool/jwt_tool/jwt_tool.py |
| wxapkg | active | native | 1.5.0 | tools/managed/wxapkg/wxapkg_1.5.0_windows_amd64.exe |
| FingerprintHub | active | **data** | main@2026-09 | tools/managed/fingerprinthub/FingerprintHub-main |

### 1.3 A3 首轮失败根因档案（已按 §8 修复，19:21 复验通过；以下为首轮实测留档）

逐名核查（含大小写不敏感复查 `jsfinder` / `fingerprinthub`，均不存在）：

- `AGENT_MANIFEST.md`（生成时间 **2026-09-07 18:47**，工作树 diff +107/-11）确实**再生过**：diff 中包含 W-4 新版 input_testing 说明与 ffuf unavailable→active 说明更新。
- `arjun` = True 的来源是新 W-4 note（manifest:221，与 `tool_strategy.json` input_testing notes 同文）。
- `wxapkg` = True 是**巧合命中**：来自既有的根目录脚本行 `decrypt_wxapkg.py` / `unpack_wmpf_wxapkg.py`（manifest:88-89），与新登记的 registry 条目 `tools/managed/wxapkg/` 无关。
- `JSFinder` / `jwt_tool` / `FingerprintHub`：三个名字在 manifest 中**零出现**。
- 根因链条：`AGENT_MANIFEST.md` 由 `scripts/gen_agent_manifest.py` 生成，source = `gov_exercise_config.json` + **`tool_strategy.json` 全部 phase 条目** + 根目录脚本表 + 桌面入口，**不含 tool_registry.json 转储**。而 `tool_strategy.json` 全文检索：`JSFinder=False, arjun=True, jwt_tool=False, wxapkg=False, FingerprintHub=False`。→ 工具只做了 registry 登记（四步之第 1、2 步），没有落进 tool_strategy 的任何 phase 引用（四步之第 3 步 mechanically 跑了，但产出必然缺名）。
- 关联事实：⑦ JSFinder 接入 application_mapping 只完成了 workflow.md 侧（`.agents/skills/wz/references/workflow.md:139` 已有 `python tools/managed/jsfinder/JSFinder.py -f <local js file or dir>` 全量解析指引），tool_strategy.json 的 application_mapping 条目未同步引用。

---

## 2. B 组 XCX 接线（全部 PASS）

| 项 | 期望 | 实测 | 判定 |
|---|---|---|---|
| B1 新 seed 含新 substatus | 新 workspace 种子含 `component_nday_screening` | `%TEMP%\xcx_p1_accept` 初始化 EXITCODE=0，33 阶段：`backend_web_api_testing` → substatuses `{"component_nday_screening": ""}`；`crypto_and_secret_handling` 同步种入 `"passive_leak_dork": ""`；`package_integrity_update_review` 七 substatus 维持空串（X-5 非 P1 范围）。audit 侧 `.agents/skills/xcx/scripts/audit_miniapp_engagement.py` 已有 `BACKEND_COMPONENT_REVIEW_BRANCHES = {"backend_web_api_testing": ("component_nday_screening",)}` 与 `BACKEND_COMPONENT_SCREEN_ARTIFACT = "artifacts/backend-component/nday-screen.jsonl"`（注释注明与 init 常量同源、由 `tests/test_xcx_backend_component_screening.py` 锁定，该测试文件存在且在全量回归中通过） | **PASS** |
| B2 三镜像三项内容 | 3 组 True True True | `.agents` / `.claude` / `.opencode` 三份 `xcx/references/workflow.md` 均 = `True True True`（component_nday_screening 或 backend-component / wxapkg / passive_leak_dork） | **PASS** |
| B3 游标隔离 | xcx 脚本不引用 WZ 游标 | 三镜像 `.agents/.claude/.opencode` 全部 `*.py` 扫描：**零 SUSPECT**（无带引号 `phase_status.json` 引用且不含 `phase_status.miniapp` 豁免的文件）。audit 脚本注释亦明示"本审计只读 miniapp 游标文件，绝不读写 WZ 的 phase_status.json" | **PASS** |

---

## 3. C 组 input_testing 预算制（全部 PASS）

| 项 | 期望 | 实测 | 判定 |
|---|---|---|---|
| C1 策略文件已改 | True | 验收表达式 → `True`；`tool_strategy.json` input_testing notes 已改为 W-4 预算制全文：orchestration-only + Tier B 单目标批次档、探测白名单（sqli_triage 浅层档 / xss_candidate_triage 惰性 marker / arjun 参数发现单 host ≤1 次 GET 优先）、预算 = 每 host ≤10 参数单发、全部探测记 `artifacts/input-testing/probe-ledger.jsonl`、非白名单脚本与超预算（>10 参数/单参多发/同 host 第二次 arjun）判违例、sqlmap 等利用级工具维持审批门。旧句 "no probe tool referenced" 仅以"被白名单取代"的历史引用形态残留 | **PASS** |
| C2 预算三态测试 | 通过/超预算拒/非白名单拒 三态齐备且全过 | `.venv pytest tests -q -k input_testing` → **30 passed**。三态用例齐备：`test_probe_budget_within_limits_passes`（通过）/ `test_probe_budget_over_params_per_host_rejected` + `test_probe_budget_multi_shot_per_param_rejected` + `test_probe_budget_second_discovery_run_rejected`（超预算拒）/ `test_probe_non_whitelist_tool_rejected`（非白名单拒）；另有 per-host 非全局、空 ledger 合法、row contract 负例、audit 集成、常量与策略契约锁定共 10 个专项用例（`tests/test_input_testing_probe_budget.py`） | **PASS** |

---

## 4. D 组契约与引擎一致性（全部 PASS）

| 项 | 期望 | 实测 | 判定 |
|---|---|---|---|
| 契约校验 | 通过 | `validate_run_contracts.py` → "run 契约校验通过：契约文件结构完整，状态模型与门控阈值无漂移"，EXITCODE=0 | **PASS** |
| X-4 键同步 | 契约与引擎不静默不一致 | `contracts/miniapp_storage_package_schema.json` 含 `passive_leak_dork` 键；引擎 `src/authorized_assessment/miniapp/crypto_secret_review.py` **同样**提及该键——两侧一致，无需援引"文档+契约层先行"豁免条款 | **PASS** |

---

## 5. E 组回归（全部 PASS）

| 项 | 期望 | 实测 | 判定 |
|---|---|---|---|
| 全量 pytest | 全绿 | `.venv pytest -q` → **1591 passed, 1 warning**（60.55s；P0 基线 1566 → 净增 25，即 P1 新增测试） | **PASS** |
| verify_offline | status ok | `--json` → 顶层 `status: ok`，EXITCODE=0（compile / skill-drift / doc-drift / tests 全过，tests 段 1591 passed） | **PASS** |
| skill drift | 零漂移 | `check_skill_drift.py` → `status: ok`，31 文件，`.claude`/`.opencode` 两镜像 missing/extra/changed 全空 | **PASS** |
| WZ audit（jlc.com） | 零新违例 | `state=REVIEW_PENDING, issues=[], open_review_items=[www-L1, www-L2, www-L3], known_vuln_triage_substatuses={}`；EXITCODE=1 为 REVIEW_PENDING 结构态基线（与 P0 完全一致），非新违例。审计后 `git status engagements/` 零改动 | **PASS** |
| XCX audit（zjlyxy-charging-xcx-20260905） | 零新违例 | `state=CLOSED, issues=[]`，EXITCODE=0，无 backend_component 新违例 | **PASS** |

---

## 6. P0 结论复核（未被 P1 改动破坏，全部符合）

| P0 关键结论 | 本会话抽查 | 结论 |
|---|---|---|
| known_vuln_triage 阶段接线 | `.agents` init_engagement.py PHASES 19 阶段，`application_mapping` 后仍为 `known_vuln_triage` | 未破坏 |
| ROE Tier A 增补 | `'Tier A' in ROE.md and 'nuclei_detect_include' in ROE.md` → True | 未破坏 |
| 白名单首版 4,139 条 | `wordlists/nuclei_detect_include.ids` 非注释行实测 **4139** | 未破坏 |
| Batch16 工具 + 三版本滚动 | ffuf 2.2.1 / xsstrike master / dalfox 3.2.2 / subfinder 2.16.0 / dnsx 1.3.1 / semgrep 1.x 全 active；nuclei 3.11.1 / templates 10.4.8 / afrog 3.5.6；codeql 仍 unavailable | 未破坏 |

---

## 7. 范围污染检查（P2 三标记物全库 grep，零命中）

| 标记物 | 对应 P2 项 | 命中 |
|---|---|---|
| `signature_replay_l0` | P2-⑧ X-2 执行器 | **0** |
| `poc_intake` | P2-⑨ 情报 ingest 流水线 | **0** |
| `VP-20260907`（模式库追加） | P2-⑨ 复利闭环首批模式条目 | **0**（`knowledge_base/vuln_pattern_lib.jsonl` 仍 10 行） |

辅助核对：`tools/managed/nuclei-custom-templates/` 目录不存在（自建模板未提前建）；`src/` 无 FingerprintHub 数据接线（P2-⑩ 数据接入未提前做；registry 以 runtime=data 登记属验收清单 A2 明确要求，不计污染）。

**结论：无 P2 范围污染。**

---

## 8. 修复交接提示词（首轮 A3 一项；已于 2026-09-07 19:21 由施工会话执行完毕并通过复验，以下为留档原文）

```markdown
# 任务：修复 P1 验收 A3 FAIL（manifest 五工具名缺三）

## 失败项
- 验收项 A3：`AGENT_MANIFEST.md` 应含 JSFinder / arjun / jwt_tool / wxapkg / FingerprintHub 五个名字（`scripts/gen_agent_manifest.py` 再生后），实测 all(...) = False。
- 期望：True。实测：JSFinder=False, arjun=True, jwt_tool=False, wxapkg=True, FingerprintHub=False。

## 根因（验收会话已定位）
- AGENT_MANIFEST.md 由 scripts/gen_agent_manifest.py 生成，source 是 tool_strategy.json 的 phase 条目
  + 根目录脚本表等，不转储 tools/tool_registry.json。manifest 已于 2026-09-07 18:47 再生过，但
  tool_strategy.json 只新增了 arjun（input_testing W-4 note），未引用 JSFinder / jwt_tool / FingerprintHub
  （wxapkg 的命中来自既有根脚本 decrypt_wxapkg.py 行，与新 registry 条目无关）。
- 关联：⑦ JSFinder 接入 application_mapping 只做了 workflow.md 侧
  （.agents/skills/wz/references/workflow.md:139 已有 JSFinder 全量解析指引），tool_strategy.json
  application_mapping 条目未同步。

## 修复要求（改 source，不手改 manifest——文件头注明"勿手改"）
1. tool_strategy.json 补工具引用（notes 层面，与 arjun 同形态）：
   - application_mapping notes += JSFinder（tools/managed/jsfinder/JSFinder.py，离线 JS chunk 全量
     端点提取，P1-6 修复，只读本地解析不发请求，产物并入 endpoint 清单并留 provenance）；
   - authenticated_testing notes += jwt_tool（tools/managed/jwt_tool/，离线解码/审计；受限篡改验证
     仅 Tier B/C 审批门内，永不自动批量）；
   - fingerprint notes += FingerprintHub（tools/managed/fingerprinthub/FingerprintHub-main，
     runtime=data 指纹备库，仅本地比对，P2 数据接入不在本次范围）；
   - 建议顺带给 wxapkg 备用解包器一个真实 phase 引用（与 xcx workflow.md §2 表 2b 行同源语义），
     消除"wxapkg 命中纯属既有脚本名巧合"的假阳性。
2. python scripts/gen_agent_manifest.py 再生。
3. 回归：python scripts/maintenance/rebuild_tool_inventory.py --check（退出码 0）；
   python scripts/check_skill_drift.py；.venv python -m pytest tests/test_tool_registry.py -q；
   .venv python -m pytest -q 全绿。
4. 复验命令（验收会话将重跑）：
   python -c "s=open('AGENT_MANIFEST.md', encoding='utf-8').read(); print(all(x in s for x in ('JSFinder','arjun','jwt_tool','wxapkg','FingerprintHub')))"
   期望 True。

## 护栏
只改 tool_strategy.json 与再生 manifest；不动 skills 三镜像、契约、src、tests；不联网；
完成后在 git diff 中自检无范围外变更。
```

---

## 9. 观察项（不影响判定，交施工/运维会话知悉）

1. **wxapkg 的 A3 命中曾是假阳性（已修复消除）**：首轮 `'wxapkg' in manifest` 来自既有根目录脚本行而非新工具条目；复验时施工会话已在 tool_strategy.json 包处理 phase notes 补充真实引用（备用解包器，与 xcx workflow §2 表 2b 同源），假阳性不再存在。
2. **PATH `python` 无 pytest**（与 P0 观察项 2 相同，未恶化）：pytest 类命令继续用 `.venv`。
3. **`.pytest_table04/` 未跟踪目录**：2026-09-02 产生（早于本次 P1 施工），内含测试临时目录残骸，与本验收及 P1 无关，仅记录；可由运维会话清理或加入 gitignore。
4. **registry 条目数 39**：规格附录 B 基线 32 + P1 五工具 = 37，另 +2 为 jadx/apktool（P0 观察项 4 已记录的并行 app 工作线在途内容），非 P1 范围，不计污染。

---

## 10. 验收命令与退出码汇总（证据索引）

```text
A1  python scripts/maintenance/rebuild_tool_inventory.py --check --json   → EXITCODE=0, violations=[], ok=true
A2  registry dump（JSFinder/arjun/jwt_tool/wxapkg/FingerprintHub）        → 5/5 active；FingerprintHub runtime=data；registry 共 39 条目
A3  manifest all(...) 五名齐备（首轮 18:47 版）                            → False（FAIL；逐名 JSFinder=F, arjun=T, jwt_tool=F, wxapkg=T, FingerprintHub=F）
    生成时间戳 2026-09-07 18:47；tool_strategy.json 五名检索仅 arjun=True
A3' manifest all(...) 复验（修复后 19:21 版）                              → True（五名齐备；tool_strategy.json 五名检索全 True 且均真实语义引用）
B1  init_miniapp_engagement.py wx0000000000000000 --output %TEMP%\xcx_p1_accept --platform wechat
                                                                          → EXITCODE=0；33 阶段；backend_web_api_testing=
                                                                            {"component_nday_screening": ""}；crypto 含 passive_leak_dork 种子
    audit_miniapp_engagement.py 常量                                       → BACKEND_COMPONENT_REVIEW_BRANCHES / nday-screen.jsonl 在位；锁定测试文件存在
B2  三镜像 workflow.md 三项内容                                            → .agents/.claude/.opencode 各 True True True
B3  三镜像 *.py 游标隔离扫描                                               → 零 SUSPECT
C1  tool_strategy.json 验收表达式                                          → True（W-4 预算制 notes 全文在位）
C2  .venv pytest tests -q -k input_testing                                 → 30 passed（三态用例齐备，tests/test_input_testing_probe_budget.py 10 专项）
D   validate_run_contracts.py                                             → EXITCODE=0 无漂移
    contracts 含 passive_leak_dork 键 / 引擎同步                            → miniapp_storage_package_schema.json 有键；crypto_secret_review.py 有键；一致
E   .venv pytest -q（首轮 / 复验）                                          → 1591 passed, 1 warning（60.55s / 64.78s，同数无回退）
    .venv scripts/verify_offline.py --json（首轮 / 复验）                  → status=ok, EXITCODE=0（两次）
    scripts/check_skill_drift.py（首轮 / 复验）                            → EXITCODE=0, status ok, 31 文件零漂移（两次）
    wz audit_engagement.py engagements/jlc.com --json（首轮 / 复验）       → state=REVIEW_PENDING, issues=[], EXITCODE=1（基线结构态，两次）
    xcx audit_miniapp_engagement.py engagements/zjlyxy-...-20260905 --json → state=CLOSED, issues=[], EXITCODE=0（两次）
    git status engagements/（首轮 / 复验）                                 → 零改动（双 audit 只读确认）
R   复验专项：rebuild_tool_inventory --check                              → violations=[], ok=true, EXITCODE=0
    复验专项：.venv pytest tests/test_tool_registry.py -q                 → 43 passed
    复验专项：mtime 扫描修复范围                                           → git 跟踪文件仅 tool_strategy.json + AGENT_MANIFEST.md（均 19:21）= 申报范围
污染 signature_replay_l0 / poc_intake / VP-20260907 全库 grep（两轮）      → 0 / 0 / 0
P0   ROE Tier A、白名单 4139、PHASES 19 阶段、Batch16 版本                  → 全部未破坏
```

**验收会话**：独立验收，2026-09-07。首轮判定 **FAIL（仅 A3）**；施工会话按 §8 修复（tool_strategy.json 四处真实引用 + 19:21 manifest 再生）后同会话复验（§0.1），A3 升格 PASS，**最终判定 PASS**，无遗留修复项。
