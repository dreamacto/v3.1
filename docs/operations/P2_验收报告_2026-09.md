# P2 验收报告（WZ/XCX 能力升级，2026-09-07）

> **验收性质**：独立验收会话，fail-closed，只判不修。一切以盘上事实为准，不采信施工会话口头声称。
> **规格源**：`docs/operations/WZ_XCX_能力升级方案_2026-09-07.md`（重点 §4 X-2、§7、§8、附录 F/H）
> **前置复核**：`docs/operations/P1_验收报告_2026-09.md` 判定为 PASS（复验后）；本会话在 E 组回归中复核其关键结论未被 P2 改动破坏（§5、§7）。
> **验收范围**：P2 五项 = ⑧ signature_replay L0 执行器（X-2 新模块）；⑨ 情报 ingest 流水线（§7.2）+ weekly-sweep 增补（§7.1/§8）+ 模式库首批 5 条（附录 H）+ 自建模板目录（附录 F）；⑩ FingerprintHub 接入（本清单无专项检查项，按盘上事实记观察项，见 §8.1）。
> **护栏遵守**：全程只读；除本报告外未修改任何项目文件；零联网（仅 B4 `nuclei -validate` 本地操作，符合护栏豁免）；双 audit 后 `git status engagements/` 零改动。

---

## 最终判定：**PASS（首轮即过，无修复项）**

- A-E 五组逐项全过；三条特别红线（白名单零改动 / 模式库既有行零改动 / L0 无默认执行无网络调用）全过；无 P2 范围污染（mtime 足迹扫描，§7）。
- 4 条观察项随报告交付（§8），均不影响判定；其中 §8.1（FingerprintHub 接入的完成度口径）建议施工/后续会话知悉并澄清。

---

## 0. 运行环境说明（验收事实的一部分）

| 项 | 值 |
|---|---|
| 仓库 | `D:\PythonSource\PythonProjects\PythonProject4`，分支 `chore/personal-offline-integrity-controls`（工作树含 P0+P1+P2 未提交变更） |
| PATH `python` | 3.14.4，无 pytest 模块（与 P0/P1 验收环境一致） |
| 项目运行时 `.venv` | 3.14.4 + pytest 9.1.1 |
| 说明 | 与 P0/P1 验收一致：涉及 pytest 的命令（A2、E 组）用 `.venv` 运行；A1/A3/A4、B、C1、D 与 skill-drift、doc-drift、双 audit 用 PATH `python` 通过。环境事实，非 P2 缺陷。 |

---

## 1. A 组 L0 执行器（全部 PASS）

| 项 | 期望 | 实测 | 判定 |
|---|---|---|---|
| A1 模块存在且形态合规 | exists / write_risk_ack / plan+ingest 均 True | 三项全 True；模块 470 行 | **PASS** |
| A2 负例测试通过 | 全绿 | `.venv pytest tests -q -k "signature_replay or replay_l0"` → **25 passed**（`tests/test_signature_replay_l0.py` 18 个用例函数，负例齐备：`test_plan_rejects_write_risk_ack_true` / `test_plan_rejects_missing_write_risk_ack_fail_closed` / `test_plan_rejects_write_method_and_business_endpoint` / `test_ingest_never_produces_confirmed` 等） | **PASS** |
| A3 三镜像含 L0 段 | 3 组 True | `.agents` / `.claude` / `.opencode` 三份 `xcx/references/workflow.md` = True True True；`.agents` 版 ：162-181 的 L0 段与 X-2 规格语义逐条一致（plan+ingest、模块零执行零请求、仅 GET+`read_only_data`、每 host ≤3、每窗重放 1 次、T+5s/T+60s、delay≥3s、`write_risk_ack=false` 无审批解锁 fail-closed、操作者 Tier C 手工重放、双窗接受=candidate/单窗=signal、confirmed 永不产生） | **PASS** |
| A4 契约 | 退出码 0 | `validate_run_contracts.py` → "run 契约校验通过…无漂移"，EXITCODE=0；`contracts/miniapp_auth_schema.json` 的 `l0_executor.constraints` 与模块常量同源核对：max_endpoints_per_host=3、min_delay_seconds=3.0、time_window_offsets_seconds=[5,60]、write_risk_ack_must_be=False、allowed_endpoint_class=read_only_data、artifact=`artifacts/miniapp/auth/signature-replay-l0.jsonl` —— 全一致 | **PASS** |

**模块本体红线审查（验收附加，通读全文）**：导入仅 `typing` / `urllib.parse`（仅解析 URL，无请求）/ `argparse` / `json` / `datetime` / `pathlib` —— **零网络调用**；执行入口仅 `if __name__ == "__main__"` 的显式 CLI（`plan` / `ingest` 子命令、参数必填），**import 无副作用、无默认执行路径**；`plan` 产出纯数据 runbook（`module_executes: false`、`executor: operator_manual`、`approval_gate: tier_c_single_candidate`），被拒配置 `executable=false` 且不展开任何 replay_steps；`ingest` 只解析结构化回填并复用 `response_baseline._credential_scan` 做凭证键拒绝；行状态枚举不含 confirmed。

---

## 2. B 组 情报流水线与白名单红线（全部 PASS；B3 见判别力说明）

| 项 | 期望 | 实测 | 判定 |
|---|---|---|---|
| B1 poc_intake 三模板 | SOURCE.md / TRIAGE.md / README.md | 三文件在位（20:02）。语义与 §7.2 一致：SOURCE=逐项登记（来源/披露日期/影响范围/利用条件，POC 原样保存不改写）；TRIAGE=三分类 detect-only / interactive / exploit-only 且**AI 只建议、操作者批准才生效**；README=六步固定流程、复现一律先走 labs/ 本地靶场 | **PASS** |
| B2 自建模板目录与示例 YAML | 存在 | `tools/managed/nuclei-custom-templates/custom-parameter-as-credential-lookup.yaml` 在位（20:03），文件头自述为附录 F 形状示例（非可直接运行模板） | **PASS** |
| B3 【红线】白名单零改动 | diff 无输出；与 P0 状态字节级一致 | **见下方判别力说明**，独立基线核验通过 | **PASS** |
| B4 nuclei 本地校验（可选） | 能跑则跑 | `nuclei.exe -validate -t tools/managed/nuclei-custom-templates/` → "All templates validated successfully"，EXITCODE=0 | **PASS** |

### B3 判别力说明与独立基线（重要）

白名单两文件当前为 **git 未跟踪**状态（P0 产出，从未 `git add`），`git diff` 对未跟踪文件**恒为空输出——验收命令 B3 按字面执行没有判别力**（无论改没改都会"通过"）。本验收改用三条独立证据核验零改动：

1. **mtime**：`nuclei_detect_include.ids` / `.txt` 均为 **2026-09-07 02:09:02**（P0 时段），远早于 P1 验收结束（19:40）与 P2 施工窗（19:45 后）——P2 期间从未被触碰；
2. **行数基线**：`.ids` 非注释行实测 **4139**，与 P0 manifest `tools/_downloads_20260907/nuclei_detect_include.manifest.json` 的 `stats.included: 4139` 及 P0/P1 报告记录完全一致；`.txt` = 4143 行 = 4139 id + 4 行政策头注释；
3. **git status**：两文件仅 `??`（未跟踪），无任何已跟踪版本可产生 diff。

**结论：白名单在 P2 期间零改动（红线守住）。** 附带发现：规格 §6 要求"白名单纳入版本管理"，当前两文件尚未纳入（随分支未提交整体待提交），见 §8.2。

---

## 3. C 组 模式库追加（全部 PASS）

| 项 | 期望 | 实测 | 判定 |
|---|---|---|---|
| C1 行数=15、新 ID 齐全、既有 10 条未动 | 15 行；VP-20260907-001..005；前 10 条 id 与历史一致 | 15 行；新增恰为 VP-20260907-001..005；5 条 schema 七字段（id/category/business_scene/hypothesis_template/test_recipe/proven_count/last_used）全齐；proven_count 与附录 H 一致（001-003/005 = 1，**004 = 2** 双目标复现）；前 10 条 id = VP-0001..0003 / VP-20260903-001..002 / VP-0006..0007 / VP-20260905-001..003 | **PASS** |
| 既有行零改动（红线） | git diff 只允许新增行 | `git diff --numstat` = **+12 / -0**（0 删除、0 改动行）；进一步将 HEAD 版本（3 行）与工作树逐行比对：**HEAD 全部行是工作树的字节级前缀**——既有行一个字节都没动。+12 = P0/P1 期未提交的 7 行 + P2 新增 5 行 | **PASS** |

辅助：文件 mtime 20:03:52（P2 施工窗），与新增 5 条的落盘时间吻合。

---

## 4. D 组 weekly-sweep 增补（全部 PASS）

| 项 | 期望 | 实测 | 判定 |
|---|---|---|---|
| D1 三镜像含双周滚动与三写闭环 | True True | `.agents` / `.claude` / `.opencode` 三份 `weekly-sweep/SKILL.md` = True True True。`.agents` 版含 §1"双周滚动职责"（每两周模板库/引擎版本轮：nuclei-templates pull + afrog 更新 + registry 版本登记 + 白名单增量提案）与 §2"三写闭环检查"（confirmed → 模式条目 `vuln_pattern_lib.jsonl` / 自建模板 `nuclei-custom-templates/` / 指纹绑定，另含 poc_intake 衔接） | **PASS** |
| D2 情报源清单落盘 | KEV/OSCS 关键词文档 | `.agents/skills/weekly-sweep/references/intel_sources.md`（20:04 新建）+ poc_intake README/SOURCE 均命中 KEV+OSCS；intel_sources.md 七源与 §7.2 监控源表逐条一致（CISA KEV 每周 / nuclei-templates release notes 每两周 / OSCS 每日 / GitHub Advisory+Topic 每周 / 先知-Seebug-奇安信 CERT 每周 / afrog pocs 每两周 / Vulhub 按需） | **PASS** |

---

## 5. E 组回归（全部 PASS；与 P0/P1 结论一致）

| 项 | 期望 | 实测 | 判定 |
|---|---|---|---|
| 全量 pytest | 全绿 | `.venv pytest -q` → **1612 passed, 1 warning**（72.25s；P1 基线 1591 → 净增 21 = P2 新增测试，无回退） | **PASS** |
| verify_offline | status ok | `.venv scripts/verify_offline.py --json` → EXITCODE=0，内部 tests 段 1612 passed | **PASS** |
| skill drift | 零漂移 | `check_skill_drift.py` → `status: ok`，**32 文件**（P1 的 31 + 新增 `weekly-sweep/references/intel_sources.md`），`.claude`/`.opencode` missing/extra/changed 全空 | **PASS** |
| doc drift | 无漂移 | `check_doc_drift.py` → "无文档漂移：所有被引用路径均存在"，EXITCODE=0 | **PASS** |
| WZ audit（jlc.com） | 与 P0/P1 基线一致 | `--json` → `state=REVIEW_PENDING, issues=[], open_review_items=[www-L1, www-L2, www-L3], known_vuln_triage_substatuses={}`；EXITCODE=1 为 REVIEW_PENDING 结构态基线（与 P0/P1 完全一致），非新违例 | **PASS** |
| XCX audit（zjlyxy-charging-xcx-20260905） | 与 P1 基线一致 | `state=CLOSED, issues=[]`，EXITCODE=0 | **PASS** |
| audit 只读性 | engagements/ 零改动 | 双 audit 后 `git status engagements/` 零输出 | **PASS** |

---

## 6. 特别红线检查（三条全过）

| 红线 | 结论 | 证据 |
|---|---|---|
| 白名单文件零改动（B3） | **守住** | §2 独立基线：mtime 停在 P0 时段 02:09 + 4139 行与 manifest stats 一致 + 未跟踪无 diff |
| 模式库既有行零改动（C1） | **守住** | §3：diff +12/-0，HEAD 3 行为工作树字节级前缀 |
| L0 执行器无默认执行路径、无网络调用 | **守住** | §1 本体审查：零网络导入；仅 `__main__` 显式 CLI；plan 纯数据、被拒配置不产出可执行步骤；`module_executes: false`；契约与测试双向锁定（`test_module_constants_locked_to_contract` / `test_constraints_are_hardcoded_red_lines`） |

---

## 7. 范围污染检查（P2 足迹 = 申报范围，无污染）

**方法**：P1 验收于 19:40 收尾，以 **19:45 为分界**做 mtime 足迹扫描（git 跟踪文件 + src/tests/scripts/contracts/knowledge_base 全目录含未跟踪）。

**P2 窗口内被修改的 git 跟踪文件（恰 15 个，全部可归位申报范围）**：

| mtime | 文件 | 归属 |
|---|---|---|
| 19:56 | `contracts/miniapp_auth_schema.json` | ⑧ l0_executor 契约段 |
| 19:57 | `scripts/maintenance/validate_run_contracts.py` | ⑧ 契约校验扩展 |
| 19:59 | `tests/test_validate_run_contracts.py` | ⑧ 锁定测试 |
| 20:00 | `tool_strategy.json` | ⑧ signature_replay 阶段 notes += L0 执行器引用（纯增量，P1 的 W-4 预算制 / JSFinder / jwt_tool / wxapkg / arjun / FingerprintHub 内容全在） |
| 20:01 | `xcx/references/workflow.md` 三镜像 | ⑧ A3 L0 段 |
| 20:03 | `knowledge_base/vuln_pattern_lib.jsonl` | ⑨ 附录 H 五条 |
| 20:04 | `weekly-sweep/SKILL.md` 三镜像 | ⑨ D1 |
| 20:04-20:05 | `wz/references/workflow.md` 三镜像 | ⑩ FingerprintHub 备库指引（:107-110）；**P0 的 known_vuln_triage 段完好**（:149 起、`nuclei_detect_include.ids` :158 在位） |
| 20:09 | `AGENT_MANIFEST.md` | 登记四步再生（tool_strategy 变更后） |

**P2 窗口内的新增文件（未跟踪）**：`src/authorized_assessment/miniapp/signature_replay_l0.py`（19:56）、`tests/test_signature_replay_l0.py`（19:58）、`tools/poc_intake/{SOURCE,TRIAGE,README}.md`（20:02）、`tools/managed/nuclei-custom-templates/custom-parameter-as-credential-lookup.yaml`（20:03）、`.agents/skills/weekly-sweep/references/intel_sources.md`（20:04）。

**P0/P1 敏感文件 mtime 复核（全部早于 P2 窗口 → 未被顺带改）**：`ROE.md` 17:35、`init_engagement.py` 17:36、`test_tool_registry.py` 17:33、`init_miniapp_engagement.py` 18:40、`tests/test_input_testing_probe_budget.py` 18:46。src/ 下 5 个已修改跟踪文件（crypto_secret_review / endpoint_offline / manual_search_helper / xcx_graph / input_testing）均为 P1 期变更，P2 窗口零触碰。三个旧未跟踪文件 `scope.py`（09-02）、`review_feedback_schema.json`（08-30）、`ffuf_dirs_small.txt`（08-30）早于 P2，非本轮产物。

**结论：无 P2 范围污染；input_testing / known_vuln_triage / ROE / 契约（除 l0_executor 增量段）等 P0/P1 内容零改动。**

---

## 8. 观察项（不影响判定，交施工/后续会话知悉）

1. **FingerprintHub 接入（⑩）的完成度口径**：验收清单未含 ⑩ 专项检查项，本项不参与 PASS/FAIL。盘上可证的接入为**文档级 + 登记**：wz workflow.md :107-110 备库指引（runtime=data、仅本地比对、永不联网拉取、喂 fingerprint_deepening 与 known_vuln_triage）+ registry 的 FingerprintHub active/data 条目（P1 已登记）。**未发现代码级/数据清洗级接入证据**：`src/`、`scripts/` 无任何引用 fingerprinthub 的 Python 文件；`knowledge_base/fp_memory.jsonl`（122 行）与 `asset_fingerprint_lib.jsonl`（239 行）中 FingerprintHub 来源记录为 **0**；`tools/managed/fingerprinthub/` 下无 P2 窗口新产物（仍是 24,959 文件的原始解压）。若施工会话声称的"接入"指数据级比对落地，则与盘上事实存在缺口，建议其澄清完成定义或将数据清理接入列入 P3；若口径即"备库指引 + 本地比对约定"，则当前交付自洽。
2. **白名单尚未纳入版本管理**：规格 §6 要求白名单"纳入版本管理"，两文件当前仍为 git 未跟踪（随分支整体未提交）。分支提交时将自然纳入；在此之前，任何依赖 `git diff` 的白名单改动检查（含 B3 原命令）都没有判别力，后续批次验收应沿用本报告 §2 的独立基线法（mtime + 行数 + manifest stats）。
3. **poc_intake/SOURCE.md 相对引用路径少一级**：`../.agents/skills/weekly-sweep/references/intel_sources.md` 从 `tools/poc_intake/` 解析到不存在的 `tools/.agents/...`，应为 `../../.agents/...`。一行修复，随手可改。
4. **本地指纹视图数据在 P2 窗口被重建**：`src/authorized_assessment/artifacts/asset_fingerprint_views/{README.md,tomcat.txt}` mtime 20:25（gitignore 本地数据，`fingerprint_ingest.py` 产物）。属施工会话本地数据操作，不在 git 管理面，仅记录。

---

## 9. 验收命令与退出码汇总（证据索引）

```text
A1  形态检查 exists/write_risk_ack/plan+ingest                              → True / True / True（470 行）
A2  .venv python -m pytest tests -q -k "signature_replay or replay_l0"      → 25 passed, 1587 deselected（负例：ack=true/缺失拒、写型拒、confirmed 拒等）
A3  三镜像 xcx workflow.md 含 L0 段                                          → True True True（.agents :162-181 与 X-2 语义一致）
A4  python scripts/maintenance/validate_run_contracts.py                    → 校验通过，EXITCODE=0
    契约 l0_executor 与模块常量同源核对                                       → 3 / 3.0 / [5,60] / False / read_only_data / artifact 路径 全一致
B1  tools/poc_intake/                                                       → SOURCE.md / TRIAGE.md / README.md 在位（20:02）
B2  tools/managed/nuclei-custom-templates/                                  → custom-parameter-as-credential-lookup.yaml（20:03，附录 F 形状）
B3  git diff --stat --（白名单两文件）                                       → 空（但两文件 git 未跟踪，命令无判别力）
    独立基线：mtime 2026-09-07 02:09 + .ids 非注释行 4139 = manifest stats    → 零改动成立（红线守住）
B4  nuclei 3.11.1 -validate -t tools/managed/nuclei-custom-templates/       → All templates validated successfully，EXITCODE=0
C1  模式库                                                                   → 15 行；VP-20260907-001..005 齐；七字段全；004 proven_count=2
    git diff --numstat                                                       → +12 / -0；HEAD 3 行 = 工作树字节级前缀（既有行零改动）
D1  weekly-sweep SKILL.md 三镜像（双周滚动 / 三写闭环）                       → True True True
D2  intel_sources.md（KEV+OSCS）                                             → 在位，七源与 §7.2 表一致
E   .venv pytest -q                                                          → 1612 passed, 1 warning（72.25s；P1 基线 1591 → +21，无回退）
    .venv scripts/verify_offline.py --json                                   → EXITCODE=0（内部 tests 1612 passed）
    scripts/check_skill_drift.py                                             → status ok，32 文件零漂移（31 + intel_sources.md）
    scripts/check_doc_drift.py                                               → 无漂移，EXITCODE=0
    wz audit engagements/jlc.com --json                                      → state=REVIEW_PENDING, issues=[], open=[www-L1..L3]，EXITCODE=1（结构态基线，与 P0/P1 一致）
    xcx audit engagements/zjlyxy-charging-xcx-20260905                       → state=CLOSED, issues=[]，EXITCODE=0
    git status engagements/（双 audit 后）                                    → 零改动
R   P2 足迹 mtime 扫描（分界 19:45）                                          → git 跟踪 15 文件全归位申报范围；P0/P1 敏感文件（ROE 17:35 / init_wz 17:36 / test_tool_registry 17:33 / init_xcx 18:40 / test_input_testing 18:46）全部早于窗口；无污染
```

**验收会话**：独立验收，2026-09-07。判定 **PASS**，无遗留修复项；4 条观察项见 §8。
